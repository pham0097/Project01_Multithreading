Three versions of the dataset are provided the number of records within each is part of the file name.
Running the larger datasets on some laptops might be too time consuming so use the smaller datasets if needed.

